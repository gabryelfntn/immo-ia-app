# immo-ia-app

Application SaaS pour agences immobilières, avec modules IA (annonces, matching, rapports de visite, relances).

## Démarrer en local

```bash
npm install
npm run dev
```

Ouvrir `http://localhost:3000`.

